<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WhereToGo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WhereToGo" parent="Base.Theme.WhereToGo" />

    <style name="WhiteTextSearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:textColor">#FFFFFF</item> <!-- White text color -->
        <item name="android:textColorHint">#FFFFFF</item> <!-- White hint color -->
        <item name="android:queryBackground">@android:color/transparent</item> <!-- Optional: make background transparent -->
    </style>

</resources>